/* Nature Tours destination detail safety/fallback layer */
(function () {
  "use strict";
  const WA = "919822339466";
  const esc = v => String(v ?? "").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;");
  function hash(v){let h=0;for(let i=0;i<String(v).length;i++)h=((h<<5)-h+String(v).charCodeAt(i))|0;return Math.abs(h)}
  async function getDestination(){const id=new URLSearchParams(location.search).get("id");if(!id||!window.DestinationEngine)return null;try{await window.DestinationEngine.load()}catch(_){}return (window.DestinationEngine.destinations||[]).find(d=>String(d.id)===String(id))||null}
  async function getHotels(destination){try{const r=await fetch("data/hotels.json?cb="+Date.now());if(r.ok){const data=await r.json();return Array.isArray(data)?data.filter(h=>String(h.destinationId)===String(destination.id)):[]}}catch(_){}return []}
  function renderHotels(destination, hotels){const page=document.getElementById("destination-page");if(!page)return;page.querySelectorAll(".destination-hotels-fixed").forEach(e=>e.remove());const section=document.createElement("section");section.className="hotel-section destination-hotels-fixed";section.innerHTML=`<h2 class="hotel-section-title">Hotels in ${esc(destination.name)}</h2><div class="hotel-grid"></div>`;const grid=section.querySelector(".hotel-grid");if(!hotels.length){grid.innerHTML='<div class="glance-card"><h3>Hotels coming soon</h3><p>We are adding verified hotels for this destination.</p></div>';page.appendChild(section);return}const used=new Set();hotels.slice(0,8).forEach((hotel,i)=>{const image=hotel.image&&!used.has(hotel.image)?hotel.image:`https://loremflickr.com/900/600/hotel,resort,${encodeURIComponent(destination.name)}?lock=${hash((hotel.id||hotel.name||i)+"-hotel")}`;used.add(image);const a=hotel.amenities||["Free WiFi","Breakfast"];const text=encodeURIComponent(`Hello Nature Tours, I would like to enquire about ${hotel.name} in ${destination.name}.`);const card=document.createElement("article");card.className="hotel-card";card.innerHTML=`<img class="hotel-photo" src="${esc(image)}" alt="${esc(hotel.name)}" loading="lazy"><div class="hotel-content"><h3>${esc(hotel.name)}</h3><p>⭐ ${esc(hotel.rating||4)} Stars</p><p class="hotel-price">₹${Number(hotel.price||0).toLocaleString("en-IN")} / night</p><p>${esc(hotel.address||destination.name)}</p><div class="hotel-amenities">${a.map(x=>`<span>${esc(x)}</span>`).join("")}</div><div class="hotel-buttons"><a class="hotel-btn" target="_blank" rel="noopener noreferrer" href="https://wa.me/${WA}?text=${text}">Send Enquiry</a><a class="hotel-btn" target="_blank" rel="noopener noreferrer" href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent((hotel.name||"")+" "+(hotel.address||destination.name))}">Google Maps</a></div></div>`;grid.appendChild(card)});page.appendChild(section)}
  function itineraryPlans(destination){
    const tags=Array.isArray(destination.tags)?destination.tags:[];
    const highlights=tags.length?tags.slice(0,6):["Local sightseeing","Scenic experiences","Cultural highlights","Leisure time"];
    const plans=[{days:2,nights:1,label:"Quick Escape"},{days:3,nights:2,label:"Short Discovery"},{days:5,nights:4,label:"Complete Experience"},{days:7,nights:6,label:"Relaxed Explorer"}];
    return plans.map(p=>({id:`${destination.id}-${p.days}d`,destination:destination.name,country:destination.country||"India",duration:`${p.days} Days / ${p.nights} Nights`,style:p.label,days:Array.from({length:p.days},(_,i)=>i===0?`Arrival, check-in and ${highlights[0]}.`:i===p.days-1?`Breakfast, free time and departure from ${destination.name}.`:`Explore ${highlights[(i-1)%highlights.length]} with local sightseeing and time to relax.`)}));
  }
  function renderItineraries(destination){
    const page=document.getElementById("destination-page");if(!page)return;
    page.querySelectorAll(".destination-itineraries").forEach(e=>e.remove());
    const section=document.createElement("section");section.className="destination-itineraries";
    section.innerHTML=`<style>.destination-itineraries{margin:32px 0}.destination-itineraries h2{margin-bottom:8px}.itinerary-plan-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.itinerary-plan{border:1px solid #e2e8f0;border-radius:16px;padding:20px;background:#fff;box-shadow:0 6px 20px rgba(15,23,42,.06)}.itinerary-plan h3{margin:6px 0}.itinerary-plan .plan-meta{font-size:13px;color:#64748b}.itinerary-plan ol{padding-left:20px}.itinerary-plan li{margin:8px 0;line-height:1.45}.itinerary-plan button{width:100%;border:0;border-radius:10px;padding:12px;background:#0f766e;color:#fff;font-weight:800;cursor:pointer}.itinerary-plan button.added{background:#166534}@media(max-width:700px){.itinerary-plan-grid{grid-template-columns:1fr}}</style><h2>🗓️ Itineraries for ${esc(destination.name)}</h2><p>Choose a trip length and build a day-by-day plan around this destination.</p><div class="itinerary-plan-grid"></div>`;
    const grid=section.querySelector(".itinerary-plan-grid");
    itineraryPlans(destination).forEach(plan=>{const card=document.createElement("article");card.className="itinerary-plan";card.innerHTML=`<span class="plan-meta">${esc(plan.style)}</span><h3>${esc(plan.duration)}</h3><ol>${plan.days.map(d=>`<li>${esc(d)}</li>`).join("")}</ol><button type="button">+ Add itinerary to Plan Cart</button>`;const b=card.querySelector("button");b.onclick=()=>{const item={id:`itinerary-${plan.id}`,sourceId:plan.id,type:"itinerary",destination:plan.destination,country:plan.country,duration:plan.duration,style:plan.style,image:destination.image||"",days:plan.days};let ok=false;if(typeof window.planCartAdd==="function")ok=window.planCartAdd(item);else{try{const key="natureToursPlanCart",items=JSON.parse(localStorage.getItem(key)||"[]");if(!items.some(x=>x.id===item.id)){items.push(item);localStorage.setItem(key,JSON.stringify(items));ok=true}}catch(_){} }b.textContent=ok?"✓ Added to Plan Cart":"✓ Already Added";b.classList.add("added");document.dispatchEvent(new CustomEvent("natureToursPlanCartUpdated"));};grid.appendChild(card)});
    page.appendChild(section);
  }
  async function repair(){const d=await getDestination();if(!d)return;renderHotels(d,await getHotels(d));renderItineraries(d)}
  document.addEventListener("DOMContentLoaded",()=>setTimeout(repair,1000));
})();
